import tw, { styled } from 'twin.macro';
import { Wrapper } from '../../common/Wrapper';
import ImageFullWidth from '../../common/ImageFullWidth/ImageFullWidth';

export const DetailsWrapper = styled(Wrapper)`
  ${tw`
    flex
    flex-col 
    text-white
  `}
`;

export const MainImage = styled(ImageFullWidth)`
  ${tw`
    
  `}
`;
